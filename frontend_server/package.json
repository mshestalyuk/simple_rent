{
  "name": "angular-backend-server",
  "version": "1.0.0",
  "description": "Backend server for Angular application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build required for backend'",
    "docker:build": "docker build -t angular-app .",
    "docker:run": "docker run -p 3000:3000 angular-app"
  },
  "keywords": ["express", "angular", "backend"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "compression": "^1.7.4",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}